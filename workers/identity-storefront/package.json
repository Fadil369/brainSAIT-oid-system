{
  "name": "brainsait-identity-storefront",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --local --config wrangler.toml",
    "deploy": "wrangler deploy --config wrangler.toml",
    "dry-run": "wrangler deploy --dry-run --config wrangler.toml"
  },
  "devDependencies": {
    "wrangler": "4.120.0"
  }
}
